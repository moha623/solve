import { CommonModule } from '@angular/common';
import { AfterViewChecked, AfterViewInit, Component, ElementRef, HostListener, OnInit, QueryList, ViewChildren } from '@angular/core';

@Component({
  selector: 'app-hybrid-scroll',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './hybrid-scroll.html',
  styleUrls: ['./hybrid-scroll.css'], // fixed typo here
})
export class HybridScroll   {
 }
